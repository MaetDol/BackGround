sizeof 연산자
==============
함수아닙니다. 연산자입니다.

예제
---------
```c
#include <stdio.h>

main() 
{
  printf("%d", sizeof(int));
  printf("%d", sizeof('1'));
}
```

문법
--------
sizeof(값 또는 타입), 리턴값 : Integer. 
구조체, 배열, 타입, 변수등 메모리를 할당하는 값은 대부분 사용할 수 있습니다.

주의사항
----------
함수가 아닌, 연산자입니다.